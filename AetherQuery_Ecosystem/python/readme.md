# AetherQuery Python Tools

## 🎯 Offline-First Подход

Этот проект следует философии **offline-first** — минимальные зависимости, максимальная надёжность.

## 📦 Что включено:

### 1. Тестовый сервер (`aetherquery_server.py`)

```sh-оболочка, не с bash(строго)
# Запуск тестового сервера
python aetherquery_server.py --port 8000

# Проверка работы
curl http://localhost:8000/health

AetherQuery_Ecosystem/
├── python/
│   ├── aetherquery_server.py    # 🎯 ТЕСТОВЫЙ СЕРВЕР (работает!)
│   ├── test_real_server.py      # 🎯 ТЕСТЕР ПОДКЛЮЧЕНИЯ (работает!)
│   └── requirements.txt         # 📦 Только необходимые зависимости
└── (остальные компоненты проекта)

В VS Code откройте в терминале оболочку не bash(default), а sh-оболочку, она должна быть (sh-5.3$)
# Активируйте venv
source venv/bin/activate

# 1. Установите зависимости
pip install -r requirements.txt

# 2. Запустите тестовый сервер
python aetherquery_server.py

# 3. Проверьте подключение
python test_real_server.py

✅ Рабочий тестовый сервер — для локальной разработки

✅ Простой тестер подключения — для проверки

✅ Минимальные зависимости — для быстрого старта

✅ Чистый код — для понятности

У вас уже есть полностью рабочая система для offline-first разработки:

✅ Сервер работает — отвечает на запросы

✅ Тесты проходят — всё проверено

✅ Код простой — легко поддерживать

✅ Зависимости минимальные — быстро устанавливается

Что работает:

📦 Тестовый сервер AetherQuery (aetherquery_server.py)

Полноценный REST API
Автоматическая документация Swagger
Все эндпоинты возвращают 200 OK
Простая установка и запуск

🔧 Инструмент проверки (test_real_server.py)

Автоматическая диагностика
Подсказки при проблемах
Проверка всех возможных конфигураций

🚀 Минимальные зависимости

Только FastAPI + Uvicorn
Ничего лишнего
Offline-first подход

Что доступно:

🌐 Сервер: http://localhost:8000
📚 Документация: http://localhost:8000/docs
🩺 Health check: http://localhost:8000/health
🔧 Выполнение запросов: POST /query

ОФФЛАЙН-ПЕРВЫЙ (OFFLINE-FIRST)
──────────────────────────────
✓ Минимум зависимостей
✓ Максимум надёжности
✓ Простота > Сложность
✓ Работает "из коробки"
✓ Расширяем при необходимости

Теперь вы можете:
1. Использовать "как есть"
2. Модифицировать под свои нужды
3. Расширять при необходимости
4. Интегрировать в свои проекты
```

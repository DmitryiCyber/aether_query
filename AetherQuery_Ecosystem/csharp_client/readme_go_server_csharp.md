Назначение компонентов
Go сервер (go_server/)
server.go - HTTP сервер на порту 8080 с endpoints:

GET /health - проверка состояния сервера

GET /query?q=... - выполнение SQL запросов

POST /query (JSON) - альтернативный формат запросов

shared.go - общая логика обработки запросов

wasm.go - код для компиляции в WASM модуль

build_wasm.sh - компиляция Go кода в .wasm файл

run_server.sh - запуск HTTP сервера

C# клиент (csharp_client/)
Program.cs - точка входа с поддержкой трех режимов:

native - подключение к Go HTTP серверу

wasm - выполнение через WASM модуль

test - тестирование обоих режимов

NativeClient.cs - HTTP клиент для работы с Go сервером

WasmClient.cs - клиент для выполнения WASM модулей

wasm_modules/ - директория для WASM файлов

🚀 Быстрый старт

1. Запуск Go сервера
   bash
   cd go_server
   ./run_server.sh

# Сервер запустится на http://localhost:8080

2. Запуск C# клиента
   bash
   cd csharp_client

# Режим Native (HTTP подключение)

dotnet run -- native

# Режим WASM (заглушка)

dotnet run -- wasm

# Тестовый режим (оба режима)

dotnet run -- test 3. Пересборка WASM модуля
bash
cd go_server
./build_wasm.sh

# Файл скопируется в csharp_client/wasm_modules/

🔧 Технические детали
Архитектура
text
┌─────────────────┐ HTTP/JSON ┌─────────────────┐
│ C# Client │◄───────────────►│ Go Server │
│ │ │ │
│ • Native mode │ │ • HTTP API │
│ • WASM mode │ │ • Query proc. │
└─────────────────┘ └─────────────────┘
│ │
│ WASM module │ Same logic
▼ ▼
┌─────────────────┐ ┌─────────────────┐
│ .wasm file │ │ shared.go │
│ (compiled Go) │◄────────────────│ (common code) │
└─────────────────┘ └─────────────────┘
Режимы работы
Native режим - C# ↔ Go HTTP сервер (реальная работа)

WASM режим - C# ↔ .wasm модуль (переносимый, изолированный)

Test режим - автоматическое тестирование обоих режимов

Форматы запросов
GET: http://localhost:8080/query?q=SELECT+\*+FROM+users

POST (JSON): {"query": "SELECT \* FROM users"}

📦 Зависимости
Go сервер
bash

# Установка зависимостей

cd go_server
go mod tidy
C# клиент
xml

<!-- В AetherQuery.csproj -->
<PackageReference Include="System.Net.Http" Version="4.3.4" />
<PackageReference Include="System.Text.Json" Version="8.0.0" />
<!-- Для реального WASM: -->
<!-- <PackageReference Include="Wasmtime" Version="14.0.0" /> -->
🧪 Тестирование
Прямые HTTP запросы
bash
# Проверка здоровья
curl http://localhost:8080/health

# Выполнение запроса (GET)

curl "http://localhost:8080/query?q=SELECT%20\*%20FROM%20users"

# Выполнение запроса (JSON POST)

curl -X POST http://localhost:8080/query \
 -H "Content-Type: application/json" \
 -d '{"query":"SELECT \* FROM users"}'
Автоматическое тестирование
bash
cd csharp_client
dotnet run -- test
🔄 Рабочий процесс разработки
Изменение логики обработки запросов:

bash

# Редактируем shared.go

cd go_server
nano shared.go

# Перезапускаем сервер

./run_server.sh

# Пересобираем WASM

./build_wasm.sh
Изменение клиентской логики:

bash

# Редактируем C# файлы

cd csharp_client
nano Program.cs

# Тестируем

dotnet run -- native
dotnet run -- wasm
⚠️ Известные ограничения
WASM режим - в текущей реализации используется заглушка

Безопасность - отсутствует аутентификация

Масштабирование - однопоточный сервер

Состояние - сервер не сохраняет состояние между перезапусками

🔮 Планы по развитию

Реализация реального WASM клиента (Wasmtime)
Поддержка реальных баз данных
Web интерфейс
Аутентификация и авторизация
Кэширование запросов
Мониторинг и метрики

📝 Лицензия
Проект разрабатывается в рамках исследования возможностей WASM и межъязыковой интеграции.
